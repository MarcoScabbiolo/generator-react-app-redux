import * as app from 'actions';

const reducer = (state = {
  loading: false,
  error: undefined,
}, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default reducer;
